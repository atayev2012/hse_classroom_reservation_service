syntax = "proto3";

package auth;

service AuthService {
    rpc Login (LoginCredentials) returns (LoginResponse);
}

message LoginCredentials {
    string email = 1;
}

message LoginResponse {
    int32 status_code = 1;
    string status = 2;
    string details = 3;
}